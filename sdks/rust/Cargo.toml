[package]
name = "statico-plugin-sdk"
version = "0.1.0"
edition = "2024"
description = "SDK for building statico plugins in Rust"
license = "MIT"
keywords = ["statico", "plugin", "sdk", "analysis", "linting"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
